const Gender = ['male', 'female'] as const;

const FacultySearchAbleFields = ['name.firstName', 'email', 'presentAddress'];
const FacultyConstants = {
    Gender,
    FacultySearchAbleFields,
};

export default FacultyConstants;
