import { TDays } from './offeredCourse.interface';

const Days: TDays[] = ['Sat', 'Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri'];

const OfferedCourseConstants = {
    Days,
};

export default OfferedCourseConstants;
